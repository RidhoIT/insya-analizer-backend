[project]
name = "insya-analyzer-backend"
version = "1.0.0"
description = "Backend for INSYA Analyzer"
requires-python = ">=3.10"
dependencies = [
    "Flask==2.3.3",
    "Flask-CORS==4.0.0",
    "Pillow==10.0.1",
    "requests==2.31.0",
    "Werkzeug==2.3.7"
]